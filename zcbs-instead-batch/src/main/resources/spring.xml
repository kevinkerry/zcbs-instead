<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:jee="http://www.springframework.org/schema/jee" 
	xsi:schemaLocation="
    http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
    http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.2.xsd
    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">
	<!--启用注解 -->
	<context:annotation-config />
	<context:component-scan base-package="com.zcbspay.platform.instead.batch"/>
	<bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">  
        <property name="locations">  
           <list>
              <value>classpath:*.properties</value>  
            </list>  
        </property>  
    </bean>
    <bean id="SpringContextHelper" class="com.zcbspay.platform.instead.batch.helper.SpringContextHelper"/> 
    <bean id="logHelper" class="com.zcbspay.platform.instead.common.utils.LogHelper"/>
	<aop:config>
	    <aop:aspect ref="logHelper">
	        <aop:pointcut id="pointcut" expression="execution(* com.zcbspay.platform.instead..*.*(..))"/>
	        <aop:before method="before"  pointcut-ref="pointcut" />
	        <aop:after method="after" pointcut-ref="pointcut" />
	        <aop:around method="around"  pointcut-ref="pointcut" />
	        <aop:after-throwing method="throwException" pointcut-ref="pointcut" throwing="ex" />
	    </aop:aspect>
	</aop:config>
	
	<bean id="urlBean" class="com.zcbspay.platform.instead.common.bean.UrlBean"> <!-- 这些配置Spring在启动时会去conn.properties中找 -->  
	    <property name="batchCollectUrl" value="${basepath}${batch.collect}" />
	    <property name="batchPayUrl" value="${basepath}${batch.pay}" />  
	    <property name="batchQueryCollectUrl" value="${basepath}${batch.queryCollect}" /> 
	    <property name="batchQueryPayUrl" value="${basepath}${batch.queryPay}" />   
	    <property name="batchImportUrl" value="${basepath}${batch.import}" />  
	    <property name="batchContractUrl" value="${basepath}${batch.contract}" />
	    <property name="encryptUrl" value="${basepath}${batch.encrypt}" />
	    <property name="decodeUrl" value="${basepath}${batch.decode}" />
	 </bean>
</beans>
